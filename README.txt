***

This project is deployed at https://energyservices-project2.onrender.com

***

The raw source files and assets used for it can be found at https://github.com/guilhermeAlmeida1/EnergyServices
To reduce the load on the server, all training was done locally beforehand.
Thus, no training/testing is running on the server or the users' machine.

You can also run the dashboard locally with python 3.x '$python project2_92686.py'
It has a dependency on {pandas, dash}, and should otherwise work.